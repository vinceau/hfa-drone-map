import "./App.scss";

import React from "react";
import { hot } from "react-hot-loader/root";

const App: React.FC = () => {
  return (
    <div className="App">
      <div>Hello world!</div>
    </div>
  );
};

export default hot(App);
